<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="LetSet is a web app created using Node">
    <meta name="author" content="Somyaranjan Rout">
    <meta name="generator" content="LetSet v1.0">
    <title>LetSet</title>
    <link rel="stylesheet" src="style.css">
</head>
<body>
    <div class="box">
        <h1> <%= KindOfDay %> </h1>
    </div>

    <div class="box">
        <% for( var i=0; i<newListItems.length; i++) { %>
            <div class="item">
                <input type="checkbox">
                <p><%=newListItems[i]%></p>
            </div>
        <% } %>

        <form class="item" method="POST" action="/">
            <input type="text" name="todo" placeholder="Add ToDo" autocomplete="off">
            <button type="submit">+</button>
        </form>
    </div>
</body>
</html>